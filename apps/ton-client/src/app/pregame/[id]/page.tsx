// このファイルは、TON（Telegram）クライアントのプレゲーム画面です。
// 共通のPreGamePageコンポーネントを使用しています。

'use client';

import { PreGamePage } from '@shout2/ui/src/pages';

export default function PreGame() {
  return <PreGamePage theme="blue" />;
}
