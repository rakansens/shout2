// このファイルは、TON（Telegram）クライアントのゲーム画面です。
// 共通のGamePageコンポーネントを使用しています。

'use client';

import { GamePage } from '@shout2/ui/src/pages';

export default function Game() {
  return <GamePage theme="blue" />;
}
