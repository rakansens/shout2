// このファイルは、TON（Telegram）クライアントの結果表示画面です。
// 共通のResultPageコンポーネントを使用しています。

'use client';

import { ResultPage } from '@shout2/ui/src/pages';

export default function Result() {
  return <ResultPage theme="blue" />;
}
